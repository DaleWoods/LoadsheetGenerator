# ImpEx for Importing Products
"#% impex.enableCodeExecution(true);"

$productCatalog=masterProductCatalog
$lang=en

$catalogVersion=catalogversion(catalog(id[default=$productCatalog]),version[default='Staged'])[unique=true,default=$productCatalog:Staged]
$supercategories=supercategories(code, $catalogVersion)[mode=append]
$unit=unit(code)[default=EA]


INSERT_UPDATE Product;code[unique=true];sapLoaded[default=False];$catalogVersion
"#% impex.includeExternalDataMedia( ""SAPLoadedData.csv"", ""UTF-8"", ',', 1, 0);"
